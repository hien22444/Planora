<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Simple Test - Planora</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1>Simple Test Page</h1>
        <div class="alert alert-info">
            <h4>User Info:</h4>
            <p><strong>User exists:</strong> <%= typeof user !== 'undefined' %></p>
            <p><strong>User name:</strong> <%= user ? user.fullName : 'No user' %></p>
            <p><strong>User role:</strong> <%= user ? user.role : 'No role' %></p>
            <p><strong>User email:</strong> <%= user ? user.email : 'No email' %></p>
        </div>
        
        <div class="mt-3">
            <a href="/customer/profile" class="btn btn-primary">Test Profile</a>
            <a href="/customer/orders" class="btn btn-success">Test Orders</a>
            <a href="/customer/debug" class="btn btn-info">Debug Info</a>
        </div>
    </div>
</body>
</html>
