<!DOCTYPE html>
<html lang="vi">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Sidebar</title>
<style>
    body {
        margin: 0;
        font-family: Arial, sans-serif;
        background-color: #ffffff; /* Nền tổng thể trắng */
    }

    /* Sidebar dọc */
    .sidebar {
        width: 220px;
        background-color: #2c3e50; /* Màu nền sidebar */
        color: #ffffff; /* Chữ sidebar luôn trắng */
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        padding: 20px;
        box-sizing: border-box;
    }

    .sidebar .user {
        margin-bottom: 30px;
        font-weight: bold;
        font-size: 18px;
    }
    .sidebar .user span {
        font-weight: normal;
        font-size: 14px;
        color: #bdc3c7; /* Chữ role nhạt hơn */
    }

    .sidebar nav a {
        color: #ffffff; /* Chữ luôn trắng */
        text-decoration: none;
        padding: 10px;
        border-radius: 4px;
        border: 1px solid #2c3e50; /* Viền cùng màu nền sidebar */
        margin-bottom: 10px;
        display: block;
        background-color: #2c3e50; /* Nền link đồng bộ với sidebar */
        transition: all 0.3s;
    }

    .sidebar nav a:hover {
        border-color: #e67e22; /* Viền nổi bật khi hover */
        background-color: #2c3e50; /* Nền vẫn đồng bộ */
        color: #ffffff; /* Chữ luôn trắng */
    }
</style>
</head>
<body>

<!-- Sidebar dọc -->
<div class="sidebar">
    <div class="user">
        Xin chào, <%= user.username %>! <span>(<%= user.role %>)</span>
    </div>
    <nav>
        <a href="/services">Xem Services</a>
        <% if(user.role === 'shop') { %>
            <a href="/services/add">Tạo Service</a>
        <% } %>

        <a href="/categories">Xem Categories</a>
        <% if(user.role === 'shop') { %>
            <a href="/categories/add">Tạo Category</a>
        <% } %>

        <a href="/logout">Đăng xuất</a>
    </nav>
</div>

</body>
</html>
