<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Test Links - Planora</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1 class="text-center mb-5">🧪 Test All Links</h1>
        
        <div class="row">
            <!-- Navigation Links -->
            <div class="col-md-6 mb-4">
                <div class="card">
                    <div class="card-header bg-primary text-white">
                        <h5><i class="fas fa-link me-2"></i>Navigation Links</h5>
                    </div>
                    <div class="card-body">
                        <div class="d-grid gap-2">
                            <a href="/customer/dashboard" class="btn btn-outline-primary">
                                <i class="fas fa-home me-2"></i>Dashboard
                            </a>
                            <a href="/customer/services" class="btn btn-outline-primary">
                                <i class="fas fa-search me-2"></i>Services
                            </a>
                            <a href="/customer/orders" class="btn btn-outline-primary">
                                <i class="fas fa-list me-2"></i>Orders
                            </a>
                            <a href="/customer/cart" class="btn btn-outline-primary">
                                <i class="fas fa-shopping-cart me-2"></i>Cart
                            </a>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Profile & Account -->
            <div class="col-md-6 mb-4">
                <div class="card">
                    <div class="card-header bg-success text-white">
                        <h5><i class="fas fa-user me-2"></i>Profile & Account</h5>
                    </div>
                    <div class="card-body">
                        <div class="d-grid gap-2">
                            <a href="/customer/profile" class="btn btn-outline-success">
                                <i class="fas fa-user me-2"></i>Profile
                            </a>
                            <a href="/customer/simple-test" class="btn btn-outline-info">
                                <i class="fas fa-bug me-2"></i>Simple Test
                            </a>
                            <a href="/customer/debug" class="btn btn-outline-warning">
                                <i class="fas fa-code me-2"></i>Debug Info
                            </a>
                            <a href="/logout" class="btn btn-outline-danger">
                                <i class="fas fa-sign-out-alt me-2"></i>Logout
                            </a>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Service Detail Test -->
            <div class="col-md-6 mb-4">
                <div class="card">
                    <div class="card-header bg-info text-white">
                        <h5><i class="fas fa-eye me-2"></i>Service Detail Test</h5>
                    </div>
                    <div class="card-body">
                        <p class="text-muted">Test service detail pages (you need to have services first):</p>
                        <div class="d-grid gap-2">
                            <a href="/customer/services" class="btn btn-outline-info">
                                <i class="fas fa-search me-2"></i>Go to Services
                            </a>
                            <small class="text-muted">Then click "Xem chi tiết" on any service</small>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Avatar Dropdown Test -->
            <div class="col-md-6 mb-4">
                <div class="card">
                    <div class="card-header bg-warning text-dark">
                        <h5><i class="fas fa-user-circle me-2"></i>Avatar Dropdown Test</h5>
                    </div>
                    <div class="card-body">
                        <p class="text-muted">Test avatar dropdown in navigation:</p>
                        <div class="alert alert-info">
                            <i class="fas fa-info-circle me-2"></i>
                            Go to any page and click on your name in the top-right corner
                        </div>
                        <div class="d-grid gap-2">
                            <a href="/customer/dashboard" class="btn btn-outline-warning">
                                <i class="fas fa-home me-2"></i>Test Dropdown
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- User Info -->
        <div class="row mt-4">
            <div class="col-12">
                <div class="card">
                    <div class="card-header bg-dark text-white">
                        <h5><i class="fas fa-info me-2"></i>Current User Info</h5>
                    </div>
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-6">
                                <p><strong>Name:</strong> <%= user ? user.fullName : 'Not logged in' %></p>
                                <p><strong>Email:</strong> <%= user ? user.email : 'N/A' %></p>
                                <p><strong>Role:</strong> <%= user ? user.role : 'N/A' %></p>
                            </div>
                            <div class="col-md-6">
                                <p><strong>Phone:</strong> <%= user ? (user.phone || 'Not set') : 'N/A' %></p>
                                <p><strong>Address:</strong> <%= user ? (user.address || 'Not set') : 'N/A' %></p>
                                <p><strong>Email Verified:</strong> 
                                    <span class="badge bg-<%= user ? (user.isEmailVerified ? 'success' : 'warning') : 'secondary' %>">
                                        <%= user ? (user.isEmailVerified ? 'Yes' : 'No') : 'N/A' %>
                                    </span>
                                </p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Test Instructions -->
        <div class="row mt-4">
            <div class="col-12">
                <div class="alert alert-primary">
                    <h6><i class="fas fa-list-check me-2"></i>Test Checklist:</h6>
                    <ul class="mb-0">
                        <li>✅ Click "Profile" → Should show profile page with your info</li>
                        <li>✅ Click avatar dropdown → Should show "Thông tin cá nhân" and "Đăng xuất"</li>
                        <li>✅ Go to Services → Click "Xem chi tiết" → Should show service detail page</li>
                        <li>✅ Update profile → Should show success message</li>
                        <li>✅ All navigation links should work</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
